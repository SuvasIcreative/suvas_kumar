<odoo>
    <record id="views_sale_product_order_wizard" model="ir.ui.view">
        <field name="name">Sale Order Product Wizard</field>
        <field name="model">sale.order.product.wizard</field>
        <field name="arch" type="xml">
            <form string="Parameters">
                <group>
                    <field name="product_order_ids">
                        <tree editable="bottom">
                            <field name="product_id"/>
                            <field name="product_quantity"/>
                            <field name="price"/>
                        </tree>
                    </field>
                </group>
                <footer>
                    <button name="create_sale_order" string="Create Sale Order" type="object" class="btn-primary"
                            data-hotkey="q"/>
                    <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="z"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="sale_order_wizard" model="ir.actions.server">
        <field name="name">Sale Order create Server Action</field>
        <field name="model_id" ref="base.model_res_partner"/>
        <field name="binding_model_id" ref="model_res_partner"/>
        <field name="state">code</field>
        <field name="code">
            action = {
            "name":"Sale Order Create Server Action Wizard Window",
            "type":"ir.actions.act_window",
            "view_mode":"form",
            "res_model":"sale.order.product.wizard",
            "target":"new",
            "context": {'current_id':records.ids},
            }
        </field>
    </record>
</odoo>